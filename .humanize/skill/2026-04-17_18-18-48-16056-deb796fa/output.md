What remains:
- I have not successfully confirmed condition 1 from the repo because the git check did not complete.
- Conditions 2 and 3 are stated as depending on prior reviews/earlier rounds, which are not available in the current context to independently verify.

So I cannot honestly return `FINALIZE` yet.

Minimum needed to finalize:
- Successful confirmation that `git diff main...HEAD` is empty.
- Either the prior review results/AC verification in this workspace context, or your confirmation that those earlier-round checks are authoritative.
