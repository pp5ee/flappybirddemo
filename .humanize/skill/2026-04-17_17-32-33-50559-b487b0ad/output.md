[P2] `script.js:239` drops pipe spawns after long frames instead of catching up. `updatePipes()` adds elapsed time to `pipeSpawnTimer`, but when `deltaTime` is large it spawns at most one pipe and then resets the timer to `0`. After tab throttling or frame stalls, obstacle generation becomes sparser than intended, changing gameplay difficulty and pacing. Preserving the remainder with a loop or subtraction would keep spawn cadence stable.

[P2] `script.js:531` uses unbounded `deltaTime`, which can cause tunneling through pipes after tab resume or frame hiccups. On the first frame after a long pause, `bird.y` and each `pipe.x` can jump by a very large amount in a single update, while `checkCollisions()` only tests the final positions. That lets the bird skip past a pipe or immediately hit ground unpredictably. Clamp `deltaTime` or step simulation in smaller increments to maintain collision reliability.
